spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    url: jdbc:postgresql://localhost:5432/testdb
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
#    show-sql: true
    flyway:
      baseline-on-migrate: true
#    properties:
#      hibernate:
#        format_sql: true

exception_message: Сущность не найдена, проверьте id=
invalid_role: У user#%d недопустимая роль - %s
time_period: 15
cron-interval: 0 0/5 8-22 * * ?
check_interval: 10
